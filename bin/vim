#!/usr/bin/env bash
# Impedir que vim se abra en otro panel (solo para tmux)

if [[ -n "$TMUX" ]]; then
    win=$(tmux display-message -p '#{window_index}')
    if [[ "$win" != "1" ]]; then
        echo "vim is only allowed in first window" >&2
        exit 1
    fi
fi

exec nvim "$@"
