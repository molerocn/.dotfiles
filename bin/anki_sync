#!/bin/bash

# notify-send "Anki" "Comenzando sincronización..."

nmcli radio wifi on
sleep 5
curl localhost:8765 -X POST -d '{
  "action": "sync",
  "version": 6
}'
sleep 5
nmcli radio wifi off

# notify-send "Anki" "Tarjetas sincronizadas"
